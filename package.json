{
  "name": "denis-ops-mini-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m backend.server",
    "build": "node scripts/build.mjs",
    "test": "node --test && python3 -m unittest tests/test_server.py"
  }
}
